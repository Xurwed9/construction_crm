# Lead Management — Дастури омӯзишӣ

Ин ҳуҷҷат тамоми модули Lead Management (Воридот) -ро барои системаи Enterprise Construction CRM шарҳ медиҳад. Ҳама матн бо забони тоҷикӣ навишта шудааст.

---

## 1. Муқаддима

Модули **Lead Management** бахшест, ки тарҳи фурӯш (Sales Funnel) -ро идора мекунад. Дар ин модул харидорони эҳтимолӣ (лидҳо) аз лаҳзаи аввали тамос то бастани шартнома ва пардохт пайгирӣ карда мешаванд.

Қисмҳои асосии модул:
- Лидҳо (Lead) — харидорони эҳтимолӣ
- Нотаҳо (Note) — ёддоштҳо дар бораи ҳар лид
- Таймлайн (Timeline) — таърихи ҳамаи амалҳо
- Канбан (Kanban) — тахтаи визуалии статусҳо
- Ҷустуҷӯ, филтр ва саҳифабандӣ

---

## 2. Чаро ҳар як файл вуҷуд дорад

Модул бо намунаи меъморӣ (architecture pattern) -и мавҷудаи лоиҳа сохта шудааст:

### `app/models/lead.py`
Ин файл моделҳои дода (data models) -ро муайян мекунад:
- **Lead** — ҷадвали асосии лидҳо (дар PostgreSQL бо номи `leads`)
- **LeadNote** — ёддоштҳои лид (ҷадвали `lead_notes`)
- **LeadTimeline** — таърихи амалҳо (ҷадвали `lead_timelines`)
- **LeadStatus** — enum-и статусҳо: `new`, `first_call`, `consultation`, `office_visit`, `presentation`, `decision`, `reservation`, `contract`, `payment`, `completed`, `lost`
- **LeadPriority** — enum-и афзалият: `low`, `medium`, `high`, `urgent`
- **STATUS_TRANSITIONS** — луғате, ки гузариши имконпазир байни статусҳоро муайян мекунад

Файл вуҷуд дорад, то сохтори додаҳо дар сатҳи пойгоҳи додаҳо муайян шавад ва дигар қисмҳои лоиҳа метавонанд ба он такя кунанд.

### `app/models/__init__.py`
Ҳама моделҳо, аз ҷумла моделҳои нави лидро содир мекунад. Ин барои `Base.metadata` зарур аст, то Alembic ва тестҳо ҳама ҷадвалҳоро бинанд.

### `alembic/versions/006_create_lead_management.py`
Ин мигратсияи Alembic аст. Вазифаи он:
- Сохтани ҷадвали `leads` бо ҳамаи сутунҳо ва индексҳо
- Сохтани ҷадвали `lead_notes`
- Сохтани ҷадвали `lead_timelines`
- Сохтани намудҳои enum: `leadstatus` ва `leadpriority`

Ҳар гуна тағйирот дар структураи пойгоҳи додаҳо танҳо тавассути мигратсия анҷом дода мешавад.

### `app/schemas/lead.py`
Ин файл схемаҳои Pydantic v2 -ро дар бар мегирад:
- **LeadCreate** — додаҳо барои сохтани лид (бо тасдиқи телефон, почта, бюджет)
- **LeadUpdate** — додаҳо барои таҳрири лид (ҳамаи майдонҳо ихтиёрӣ)
- **LeadRead** — намоиши пурраи лид
- **LeadShortRead** — намоиши кӯтоҳи лид (барои рӯйхат ва канбан)
- **LeadListResponse** — ҷавоби саҳифабандишуда
- **LeadMoveRequest** — дархости тағйири статус
- **LeadAssignManagerRequest** — дархости таъини менеҷер
- **LeadNoteCreate / LeadNoteUpdate / LeadNoteRead** — схемаҳои нотаҳо
- **LeadTimelineRead** — схемаи таймлайн

Схемаҳо вуҷуд доранд, то маълумотҳо дар вуруд (input) ва хуруҷ (output) тасдиқ ва стандартикунонида шаванд.

### `app/repositories/lead.py`
Ин файл логикаи пойгоҳи додаҳоро дар бар мегирад:
- **LeadRepository** — амалҳои асосӣ барои Lead: `get_by_id`, `create`, `update`, `soft_delete`, `list`, `kanban`
- **LeadNoteRepository** — амалҳо барои LeadNote
- **LeadTimelineRepository** — амалҳо барои LeadTimeline
- **LeadReferenceRepository** — санҷиши мавҷудияти менеҷер/лоиҳа/бино

Репо вуҷуд дорад, то ҳамаи саволҳо (queries) ва амалиёти SQL дар як ҷо ҷамъ бошанд.

### `app/services/lead.py`
Ин файл логикаи тиҷоратӣ (business logic) -ро дар бар мегирад:
- Эҷод ва таҳрири лид бо санҷиши мансубият (ownership)
- Ҳаракати статус бо санҷиши гузариш
- Таъини менеҷер
- Идораи нотаҳо
- Эҷоди таймлайн барои ҳар амал
- Канбан

Сервис вуҷуд дорад, то логика дар рутерҳо набошад ва рутерҳо борик бошанд.

### `app/api/v1/endpoints/leads.py`
Ин файл рутерҳои HTTP -ро муайян мекунад:
- Тамоми endpoint-ҳои модули лид дар ин ҷо ҷамъ шудаанд
- Рутер танҳо параметрҳоро мегирад ва ба сервис мефиристад

### `app/permissions/roles.py`
Ин файл ҳуқуқҳои дастрасиро муайян мекунад:
- `LEADS_CREATE`, `LEADS_VIEW`, `LEADS_UPDATE`, `LEADS_DELETE`, `LEADS_MOVE`, `LEADS_ASSIGN`, `LEADS_NOTES`
- Менеҷер танҳо ба лидҳои худаш дастрасӣ дорад
- Клиент ҳеҷ дастрасие надорад

### `app/main.py`
Рутери лидҳоро ба FastAPI насб мекунад: `app.include_router(leads.router, prefix="/api/v1")`

---

## 3. Модули Lead чӣ гуна кор мекунад

### Офаридани лид (Create Lead)
1. Корбар `POST /api/v1/leads` -ро мефиристад
2. Рутер схемаи `LeadCreate` -ро қабул мекунад (телефон, почта, бюджет санҷида мешаванд)
3. Сервис `create_lead` -ро даъват мекунад:
   - Агар корбар **менеҷер** бошад, лид ба худи ӯ таъин мешавад
   - Агар корбар **админ** бошад, вай метавонад менеҷери дигар таъин кунад
   - Лоиҳа/бино/хона агар дода шуда бошанд, мавҷудияташон санҷида мешавад
4. Майдони `full_name` автоматикӣ ҳисоб карда мешавад: "Ҷон Смит"
5. Барои лид вуруди таймлайн `lead_created` сохта мешавад

### Гирифтани лид (Get Lead)
`GET /api/v1/leads/{id}` — лидро бо ҳамаи маълумотҳо, аз ҷумла менеҷери таъиншуда бармегардонад. Менеҷер танҳо лиди худашро гирифта метавонад (дар акси ҳол 403).

### Рӯйхати лидҳо (List Leads)
`GET /api/v1/leads` — бо саҳифабандӣ, ҷустуҷӯ, филтрҳо ва тартибдиҳӣ.

### Таҳрир (Update)
`PATCH /api/v1/leads/{id}` — танҳо майдонҳои фиристодашуда тағйир меёбанд. Агар ном тағйир ёбад, `full_name` аз нав ҳисоб мешавад.

### Ҳазф (Delete)
`DELETE /api/v1/leads/{id}` — ҳазфи нарм (soft delete): лид дар пойгоҳи додаҳо мемонад, аммо `deleted_at` гузошта мешавад ва дигар дар рӯйхатҳо пайдо намешавад. Ин таърихро нигоҳ медорад.

---

## 4. Канбан чӣ гуна кор мекунад

Канбан тахтаест, ки лидҳоро аз рӯи статус гурӯҳбандӣ мекунад.

`GET /api/v1/leads/kanban` чунин ҷавоб медиҳад:

```json
{
  "new": [{"id": "...", "full_name": "Ҷон Смит", "status": "new", "...": "..."}],
  "first_call": [],
  "consultation": [...],
  "reservation": [...],
  "lost": []
}
```

Хусусиятҳо:
- Ҳамаи 11 статус ҳамеша ҳузур доранд (ҳатто холӣ), то фронтенд сутунҳои доимиро нигоҳ дорад
- Дар ҳар сутун лидҳо бо фармоиши афзалият (urgent → high → medium → low) ва рӯзи эҷод ҷобаҷо мешаванд
- Филтрҳо (менеҷер, лоиҳа, ҷустуҷӯ ва ғайра) дар канбан низ кор мекунанд
- Менеҷер танҳо сутунҳои лидҳои худашро мебинад

---

## 5. Таймлайн чӣ гуна кор мекунад

Таймлайн (таърих) дар ҷадвали `lead_timelines` нигоҳ дошта мешавад. Ҳар амал як сабт эҷод мекунад:

| Амал | action дар пойгоҳи додаҳо |
|------|---------------------------|
| Лид сохта шуд | `lead_created` |
| Лид таҳрир шуд | `lead_updated` |
| Лид ҳазф шуд | `lead_deleted` |
| Статус тағйир ёфт | `status_changed` |
| Менеҷер таъин шуд | `manager_assigned` |
| Нота илова шуд | `note_added` |
| Нота таҳрир шуд | `note_updated` |
| Нота ҳазф шуд | `note_deleted` |

Ҳар вуруди таймлайн дорои: `action`, `description` (шарҳ), `actor_id` (ки амал кард) ва `created_at`.

Функсияи `_add_timeline` дар сервис ҳамаи ин амалҳоро иҷро мекунад. Он ҳамчунин майдони `last_activity_at` -и лидро нав мекунад.

---

## 6. Гузариши статус чӣ гуна кор мекунад

Ҳар статус танҳо ба статусҳои муайян гузашта метавонад. Ин дар луғати `STATUS_TRANSITIONS` муайян шудааст:

```
new          → first_call, lost
first_call   → consultation, new, lost
consultation → office_visit, first_call, lost
office_visit → presentation, consultation, lost
presentation → decision, office_visit, lost
decision     → reservation, presentation, lost
reservation  → contract, decision, lost
contract     → payment, reservation, lost
payment      → completed, contract, lost
completed    → (ягон гузарише нест — статуси ниҳоӣ)
lost         → new (аз нав кушодан)
```

Хусусиятҳо:
- Гузариши пешрафта (forward) барои тарҳи фурӯш
- Гузариши бозгашт ба як қадам барои ислоҳи хатогиҳо
- Ба `lost` аз ҳама ҷо гузаштан мумкин
- Аз `lost` танҳо ба `new` гузаштан мумкин (эҳёи лид)
- `completed` — статуси ниҳоӣ
- Агар гузариш имконнопазир бошад, хатогии **400** баргардонида мешавад

Мисол: агар лид дар статуси `new` бошад ва шумо ба `contract` гузаред, сервер 400 медиҳад, зеро ин гузариш имконнопазир аст.

---

## 7. Сервисҳо (Services) чӣ гуна кор мекунанд

Сервисҳо қабати логикаи тиҷоратӣ мебошанд. Онҳо байни рутерҳо ва репозиториҳо ҷойгиранд:

```
Router (HTTP) → Service (логика) → Repository (SQL)
```

Нақши сервис:
- Санҷиши мавҷудияти лид (`NotFound`)
- Санҷиши мансубият (менеҷер танҳо лидҳои худаш)
- Тасдиқи гузариши статус
- Ҳисоб кардани `full_name`
- Эҷоди таймлайн барои ҳар амал
- Тасдиқи иртибот ба лоиҳа/бино/хона

Рутерҳо бориканд: онҳо танҳо параметрҳоро мегиранд, ба сервис мефиристанд ва ҷавобро бармегардонанд.

---

## 8. Намунаи Репозиторий (Repository Pattern) чӣ гуна кор мекунад

Репозиторий қабати пойгоҳи додаҳост. Ҳеҷ логикаи тиҷоратӣ дар он нест — танҳо SQL ва сессия:

- **Репо мефаҳмад**, ки ҷадвалҳо чӣ гуна кор мекунанд
- **Репо ҳеҷ намедонад**, ки корбар кист ва чӣ ҳуқуқ дорад
- **Репо** саволҳоро (queries) месозад: филтр, тартиб, саҳифабандӣ

Манфиатҳо:
- Код такрор намешавад
- Тағйири пойгоҳи додаҳо (масалан SQLite ↔ PostgreSQL) танҳо дар репо
- Санҷиши осон: репоҳоро алоҳида санҷидан мумкин
- Логикаи тиҷоратӣ ва SQL аз ҳам ҷудоанд

---

## 9. Шарҳи ҳар endpoint

### Лидҳо

| Метод | Роҳ | Шарҳ |
|-------|-----|------|
| `POST` | `/api/v1/leads` | Лиди нав месозад. Код 201. Барои менеҷер лид ба худи ӯ таъин мешавад |
| `GET` | `/api/v1/leads` | Рӯйхати лидҳо бо саҳифабандӣ, ҷустуҷӯ, филтр ва тартиб |
| `GET` | `/api/v1/leads/kanban` | Канбан: лидҳо аз рӯи статус гурӯҳбандӣ шудаанд |
| `GET` | `/api/v1/leads/{id}` | Лид бо маълумоти пурра (бо менеҷер) |
| `PATCH` | `/api/v1/leads/{id}` | Таҳрири қисмии лид |
| `DELETE` | `/api/v1/leads/{id}` | Ҳазфи нарми лид |
| `PATCH` | `/api/v1/leads/{id}/status` | Тағйири статус бо санҷиши гузариш |
| `PATCH` | `/api/v1/leads/{id}/manager` | Таъин/тағйири менеҷер (танҳо админ) |

### Нотаҳо

| Метод | Роҳ | Шарҳ |
|-------|-----|------|
| `GET` | `/api/v1/leads/{id}/notes` | Рӯйхати нотаҳои лид |
| `POST` | `/api/v1/leads/{id}/notes` | Нотаи нав месозад. Код 201 |
| `PATCH` | `/api/v1/leads/notes/{note_id}` | Таҳрири нота (танҳо муаллиф ё админ) |
| `DELETE` | `/api/v1/leads/notes/{note_id}` | Ҳазфи нота |

### Таймлайн

| Метод | Роҳ | Шарҳ |
|-------|-----|------|
| `GET` | `/api/v1/leads/{id}/timeline` | Таърихи ҳамаи амалҳо аз рӯи замон |

---

## 10. Ҳуқуқҳои дастрасӣ (Permissions)

| Нақш | Ҳуқуқҳо |
|------|---------|
| **super_admin / admin** | Ҳама: эҷод, намоиш, таҳрир, ҳазф, ҳаракати статус, таъини менеҷер, нотаҳо |
| **manager** | Эҷод, намоиш, таҳрир, ҳаракати статус, нотаҳо — танҳо ба лидҳои худаш |
| **client** | Ҳеҷ дастрасӣ (403) |

---

## 11. Хулоса

Модули Lead Management ба таври пурра сохта шудааст:
- Ҳамаи 11 статуси тарҳи фурӯш
- Канбан бо гурӯҳбандӣ
- Таймлайн барои ҳар амал
- Санҷиши гузариши статус
- Ҳуқуқҳои дастрасӣ барои ҳар нақш
- Ҷустуҷӯ, филтр, тартибдиҳӣ ва саҳифабандӣ
- Тасдиқи телефон, почта, бюджет, статус, афзалият
- Хатогиҳои HTTP ба тарзи дуруст (400, 401, 403, 404, 409, 422)

---

# Deal Management — Дастури омӯзишӣ

Ин қисмат модули **Deal Management (Муомилаҳо)** -ро шарҳ медиҳад. Деал як раванди фурӯши ягон квартира аст, ки аз лид сар шуда, то фурӯши пурра (шартнома + пардохт) идома мекунад. Ҳама матн бо забони тоҷикӣ навишта шудааст.

---

## 1. Муқаддима

Деал — ҳалқаи байни **Lead** (харидори эҳтимолӣ) ва **Apartment** (квартира) дар CRM мебошад.

Ҷараёни асосӣ:

```
Lead
  ↓  Менеҷер лидро мекушояд
  ↓  Квартираро интихоб мекунад
  ↓  Деал месозад
  ↓  Квартира автоматикӣ RESERVED мешавад
  ↓  Музокирот, ҳуҷҷатҳо, пардохтҳо
  ↓  Шартнома
  ↓  SOLD (фурӯхта шуд)
```

Деалҳо аз лидҳо сохта мешаванд, аммо метавонанд бе лид низ (мустақим барои клиент) сохта шаванд.

Модул дар дохили меъмории мавҷудаи лоиҳа сохта шудааст ва ҳеҷ сохтори нав наовардааст:

```
app/models/          → app/models/deal.py, deal_activity.py, deal_document.py, deal_payment.py, deal_task.py, deal_timeline.py
app/schemas/         → app/schemas/deal.py ва deal_*.py
app/repositories/    → app/repositories/deal.py ва deal_*.py
app/services/        → app/services/deal.py ва deal_*.py
app/api/v1/endpoints/→ app/api/v1/endpoints/deals.py
app/dependencies/    → app/dependencies/deals.py
app/permissions/     → app/permissions/roles.py (ҳуқуқҳои нав илова шуданд)
alembic/versions/    → 007_create_deal_management.py
```

---

## 2. Моделҳо (Models) — чӣ гуна сохта шудаанд

### `app/models/deal.py` — Деал ва энумҳо

Ин файл ҷадвали асосии `deals` ва энумҳои онро муайян мекунад:

| Сутун | Шарҳ |
|-------|------|
| `id` | UUID — калиди асосӣ |
| `deal_number` | Рақами деал, ба таври автоматикӣ: `DL-2026-ABC123` |
| `lead_id` | Иртибот ба лид (FK → `leads.id`) |
| `client_id` | Иртибот ба клиент (FK → `users.id`) |
| `manager_id` | Иртибот ба менеҷер (FK → `users.id`) |
| `apartment_id` | Квартира (FK → `apartments.id`) |
| `project_id`, `building_id`, `section_id`, `floor_id` | Иртибот ба матритса (FK → matrix) |
| `status` | Статуси деал (энум) |
| `priority` | Афзалият: `low`, `medium`, `high`, `urgent` |
| `payment_type` | Намуди пардохт: `cash`, `installment`, `mortgage`, `mixed` |
| `reservation_until` | Мӯҳлати нигоҳдошт (резерв) |
| `reservation_expired` | Нишондиҳандаи гузаштани мӯҳлати резерв |
| `contract_number` | Рақами шартнома |
| `price`, `discount`, `final_price` | Нарх, тахфиф, нархи ниҳоӣ |
| `paid_amount`, `remaining_amount` | Маблағи пардохтшуда ва боқимонда |
| `currency` | Асъор (USD) |
| `expected_close_date`, `closed_at` | Санаи пешбинишуда ва санаи пӯшидани деал |
| `cancel_reason` | Сабаби лағв |
| `deleted_at` | Ҳазфи нарм (soft delete) |

**Энуми статусҳо:** `new`, `consultation`, `meeting`, `negotiation`, `reserved`, `contract`, `installment`, `mortgage`, `sold`, `cancelled`.

**Чаро энумҳо бо арзишҳои хурд?** Мисли модули матритса ва лид, ҳама арзишҳои энум бо ҳарфҳои хурд нигоҳ дошта мешаванд (`values_callable`), то бо PostgreSQL ва SQLite мутобиқат кунанд.

**Индекси махсус:** `uq_deals_active_apartment` — индекси қисман уникалӣ (partial unique index). Он кафолат медиҳад, ки барои як квартира танҳо **як деали фаъол** вуҷуд дошта бошад. Деалҳои `sold` ва `cancelled` истисно ҳастанд, зеро онҳо дигар фаъол нестанд. Ин маҳз барои чӣ кор фармуда мешавад: барои квартираи фурӯхташуда метавон таърихи деалро нигоҳ дошт.

### `app/models/deal_timeline.py` — Таймлайн

Ҳар як тағйироти муҳим дар деал дар ин ҷо сабт мешавад:

- `event` — номи рӯйдод (масалан `deal_created`, `reservation`, `status_changed`)
- `old_value` / `new_value` — арзиши кӯҳна ва нав (ки чӣ тағйир ёфт)
- `performed_by` — кӣ амал кард (FK → `users.id`)
- `created_at` — кай

Ин ҷадвал **таърихи пурраи деал** (audit log) мебошад. Класси `DealEvent` ҳамаи номҳои рӯйдодҳоро ҳамчун доимӣ (constant) муайян мекунад, то ҳеҷ хатои имлоӣ набошад.

### `app/models/deal_activity.py` — Активитҳо (амалҳо)

Активитҳо тамоси мушаххас бо клиент мебошанд: занг, мулоқот, Telegram, WhatsApp, почта, SMS, ёддошти дохилӣ/оммавӣ, вазифа, ёддошт-даррез.

- `activity_type` — намуди амал (энум)
- `content` — матн
- `is_public` — агар `true` бошад, клиент низ онро мебинад; агар `false` бошад, танҳо ходимон
- `scheduled_at`, `completed`, `completed_at` — барои вазифаҳои дарпешистода
- `performed_by` — кӣ амал кард

**Чаро ҳам активит ва ҳам таймлайн?** Таймлайн сабти автоматикии система аст (система навиштааст). Активит вуруди корбар аст (менеҷер навиштааст). Ин ду ҷадвали гуногун.

### `app/models/deal_task.py` — Вазифаҳо

Вазифаҳои дохилӣ барои деал: ном, тавсиф, мӯҳлат (deadline), афзалият, иҷрошуда ё не. Ба менеҷери дигар таъин карда мешавад (`assigned_to`).

### `app/models/deal_payment.py` — Пардохтҳо

Ҳар пардохт: маблағ (`amount`), усули пардохт (`cash`, `transfer`, `card`, `bank`, `other`), санаи пардохт (`paid_at`), шарҳ.

### `app/models/deal_document.py` — Ҳуҷҷатҳо

Ҳуҷҷатҳои боркардашуда: намуд (`passport`, `contract`, `receipt`, `invoice`, `mortgage`, `photo`, `blueprint`, `pdf`), номи аслӣ, роҳи файл, андоза, намуди MIME.

### Иртиботҳо (Relationships)

Ҳамаи иртиботҳо бо SQLAlchemy `relationship` сохта шудаанд:

```
Deal 1───N DealTimeline     (back_populates="deal", cascade delete)
Deal 1───N DealActivity     (back_populates="deal", cascade delete)
Deal 1───N DealTask         (back_populates="deal", cascade delete)
Deal 1───N DealDocument     (back_populates="deal", cascade delete)
Deal 1───N DealPayment      (back_populates="deal", cascade delete)
Deal N───1 Lead/User/Apartment/Project/Building/Section/Floor  (lazy="selectin")
```

`lazy="raise"` барои коллексияҳо гузошта шудааст, то сабукфикрии аварийӣ (accidental lazy load) нашавад ва ҳамааш дар репо ошкоро бор карда шавад. `lazy="selectin"` барои иртиботҳои ягона, то ҳангоми хондан ҳамаи маълумоти зарурӣ якҷоя гирифта шавад.

---

## 3. Мигратсия (007_create_deal_management.py)

Ин файли Alembic ҳамаи ҷадвалҳои болоро месозад: `deals`, `deal_timelines`, `deal_activities`, `deal_tasks`, `deal_documents`, `deal_payments` бо ҳамаи индексҳо ва калидҳои хориҷӣ. Индекси қисман уникалӣ танҳо барои PostgreSQL (`postgresql_where`) сохта мешавад, зеро SQLite версияи кӯҳнаи partial index дорад (дар модели модел `sqlite_where` низ ҳаст, то тестҳо кор кунанд).

**Чаро UUID?** Барои системаҳои тақсимшуда ва барои надоштани ихтилофи ID-ҳо. Дар тамоми лоиҳа UUID истифода мешавад.

---

## 4. Схемаҳо (Pydantic v2)

### `app/schemas/deal.py`
- **DealCreate** — вуруди эҷоди деал. Санҷиши тиҷоратӣ: тахфиф наметавонад аз нарх зиёд бошад.
- **DealUpdate** — таҳрири қисмӣ. Ҳамаи майдонҳо ихтиёрӣ.
- **DealReserveRequest** — мӯҳлати резерв (ихтиёрӣ).
- **DealCancelRequest** — сабаби лағв (ҳатмӣ).
- **DealCloseRequest** — рақами шартнома (ихтиёрӣ).
- **DealRead** — намоиши пурраи деал бо иртиботҳо (lead, client, manager, apartment, project, building).
- **DealListResponse** — ҷавоби саҳифабандишуда.

### `app/schemas/deal_statistics.py`
**DealStatistics** ва **ManagerPerformance** — натиҷаҳои омор: шумораи умумӣ, кушода, резервшуда, фурӯхта, лағвшуда, имрӯз, ин моҳ, даромад, қимати миёна, меъёри табдил, топ-менеҷерон, вақти миёнаи бастани деал.

### `app/schemas/deal_dashboard.py`
**DashboardDeals** — маълумоти асосии панели идоракунӣ: деалҳои охирин, амалҳои охирин, пардохтҳои охирин, вазифаҳои имрӯза, вазифаҳои дермонда, мулоқотҳои дарпешистода, резервҳои мӯҳлаташон тамомшуда.

---

## 5. Қабати Репозиторий

### `app/repositories/deal.py`
**DealRepository** — ҳамаи амалҳои SQL барои деал:
- `get_by_id`, `get_short_by_id` — гирифтани деал (бо иртиботҳо ё бидуни онҳо)
- `get_active_by_apartment` — деали фаъол барои квартира (барои санҷиши дубликат)
- `list_overdue_reservations` — резервҳои мӯҳлаташон гузашта (барои бекор кардани автоматикӣ)
- `list_reservation_expiring` — резервҳои ба мӯҳлат наздик
- `count` — шумора бо филтрҳо (статус, сана, менeҷер, клиент)
- `get_revenue` — ҷамъи нархи ниҳоии деалҳои фурӯхта
- `get_top_managers` — рейтинги менеҷерон
- `get_avg_closing_time_days` — вақти миёнаи бастани деал
- `create`, `update`, `soft_delete`, `list`

Функсияи `build_deal_filters` ҳамаи филтрҳоро месозад: ҷустуҷӯ (бо номи клиент, телефон, рақами квартира, рақами деал, рақами шартнома, номи менеҷер, лоиҳа, бино), статус, афзалият, менеҷер, лоиҳа, бино, квартира, намуди пардохт, сана.

Функсияи `apply_deal_sorting` тартибдиҳиро иҷро мекунад: `newest`, `oldest`, `price`, `remaining`, `created`, `updated`.

**Принсип:** дар репо ҳеҷ логикаи тиҷоратӣ нест. Филтри дастрасии корбар (менeҷер танҳо деалҳои худ) аз ҷониби сервис гузаронида мешавад.

### Дигар репозиторийҳо
- `deal_timeline.py` — сабт ва хондани таймлайн
- `deal_activity.py` — CRUD активитҳо, рӯйхати охирин, мулоқотҳои дарпешистода
- `deal_task.py` — CRUD вазифаҳо, вазифаҳои имрӯза/дермонда
- `deal_payment.py` — CRUD пардохтҳо, пардохтҳои охирин
- `deal_document.py` — CRUD ҳуҷҷатҳо
- `deal_references.py` — санҷиши мавҷудияти клиент/менeҷер/лид/лоиҳа/бино/қисм/ошёна (бо нақшҳои дуруст: клиент бояд нақши CLIENT дошта бошад, менеҷер бояд нақши менeҷер/админ дошта бошад)

---

## 6. Қабати Сервис

### `app/services/deal.py` — логикаи асосии тиҷоратӣ

Ин файл бузургтарин файли сервиси деал аст. Он ҳамаи қоидаҳои тиҷоратиро дорост:

**Санҷишҳои умумӣ (validations):**
- `validate_apartment_not_sold` — квартираи фурӯхташударо резерв кардан мумкин нест
- `validate_apartment_not_blocked` — квартираи блокшударо фурӯхтан мумкин нест
- `validate_no_duplicate_active_deal` — барои як квартира наметавонад ду деали фаъол бошад
- `validate_reservable_deal_status` — резерв танҳо аз статусҳои пеш-резерв (new, consultation, meeting, negotiation) мумкин аст
- `validate_sellable_deal_status` — бастани деал танҳо аз reserved/contract/installment/mortgage
- `validate_deal_not_final` — деали фурӯхтаро тағйир додан мумкин нест
- `validate_price_and_discount` — нарх манфӣ нест, тахфиф аз нарх зиёд нест
- `validate_payment_amount` — пардохт аз боқимонда зиёд нест

**Амалҳои асосӣ:**
- `create_deal` — эҷоди деал. Агар корбар менeҷер бошад, менeҷери деал худи ӯ мешавад. Агар квартира интихоб шуда бошад, нарх аз квартира мегирад, санҷиши дубликат ва фурӯхтанашудагӣ. Рақами деал (`deal_number`) автоматикӣ сохта мешавад. Сабти таймлайн `deal_created` ва `lead_converted`.
- `update_deal` — таҳрир. Агар нарх/тахфиф тағйир ёбад, `final_price` ва `remaining_amount` аз нав ҳисоб мешаванд. Тағйири статус бо санҷиши гузариш. Тағйири квартира танҳо дар статусҳои пеш-резерв.
- `delete_deal` — ҳазфи нарм. Квартира (агар резервшуда бошад) озод карда мешавад.
- `reserve_deal` — **резерватсия**: статус ба `reserved` меравад, квартира дар матритса `reserved` мешавад, мӯҳлати резерв муқаррар мешавад (бо нобаёнӣ 7 рӯз).
- `cancel_deal` — лағв бо сабаб. Квартира озод мешавад (бо `deal_id` -и квартира санҷида мешавад, ки ин деал воқеан резерв кардааст).
- `close_deal` — бастани деал: статус `sold`, `closed_at` муқаррар, рақами шартнома автоматикӣ (`CNTR-2026-XXXXXX`), квартира `sold` мешавад.
- `restore_deal` — эҳёи деали лағвшуда: статус `new`, сабаби лағв тоза мешавад.
- `add_payment` — пардохт месозад ва `paid_amount` / `remaining_amount` -ро нав мекунад.
- `expire_overdue_reservations` — резервҳои мӯҳлаташон гузаштаро бекор мекунад (квартира озод мешавад, `reservation_expired = True`). Ин дар оғози ҳар даъват (лист, гирифтан, резерв, статистика) иҷро мешавад — ҳеҷ cron лозим нест.
- `_ensure_access` — назорати дастрасӣ: админ ҳамаро мебинад, менeҷер танҳо деалҳои худ, клиент танҳо деалҳои худ (танҳо барои хондан).

**Чаро логикаи тиҷоратӣ дар сервис?** Барои SOLID ва Clean Architecture. Рутер борик аст, репо танҳо SQL мекунад, сервис қарорҳои тиҷоратиро мегирад.

### `app/services/deal_reservation.py` — Резерватсияи квартира

Ин сервис **танҳо** мутобиқати квартираро идора мекунад (матритса):
- `hold` — квартира `reserved` мешавад + `deal_id` гузошта мешавад
- `release` — квартира `available` мешавад + `deal_id` тоза мешавад
- `mark_sold` — квартира `sold` мешавад + `deal_id` гузошта мешавад

Ин ҷудокунии масъулият аст: сервиси деал медонад **ки чӣ кор кардан лозим**, резерв-сервис медонад **чӣ гуна** дар матритса.

### `app/services/deal_timeline.py` — Таймлайн

Функсияи `dispatch` ҳар рӯйдодро дар ҷадвали `deal_timelines` сабт мекунад ва ба лог низ менависад (JSON formatter дар `app/utils/logging.py`). Ин хусусияти "audit log" аст: барои ҳар рӯйдод маълум аст ки, кай, чӣ, аз кадом арзиш ба кадом арзиш тағйир ёфт.

### Дигар сервисҳо
- `deal_activity.py` — CRUD активитҳо бо санҷиши муаллиф (танҳо муаллиф ё админ тағйир дода метавонад). Ҳангоми таҳрир `old_value` ва `new_value` дуруст сабт мешаванд (қабл аз тағйирот гирифта мешаванд).
- `deal_task.py` — CRUD вазифаҳо. Таъини иҷрокунанда танҳо ба менeҷерҳо. Вақте вазифа иҷро мешавад, `completed_at` гузошта мешавад ва таймлайн сабт мешавад.
- `deal_document.py` — боркунии ҳуҷҷатҳо (ҳадди андоза 50 МБ), нигоҳдорӣ дар `uploads/deals/{deal_id}/`, тозакунии файл ҳангоми ҳазф.
- `deal_analytics.py` — ҳисобкунии омор.
- `deal_dashboard.py` — ҷамъоварии маълумоти дашборд.

---

## 7. Гузариши статус (Status Transitions)

Ҳар статус танҳо ба статусҳои муайян гузашта метавонад (`STATUS_TRANSITIONS`):

```
new          → consultation, meeting, negotiation, cancelled
consultation → new, meeting, negotiation, cancelled
meeting      → consultation, negotiation, reserved, cancelled
negotiation  → meeting, consultation, reserved, cancelled
reserved     → negotiation, contract, cancelled
contract     → installment, mortgage, reserved, cancelled
installment  → contract, mortgage, cancelled
mortgage     → contract, installment, cancelled
sold         → (статуси ниҳоӣ)
cancelled    → new (эҳё)
```

**Хусусияти махсус:** ба `reserved`, `sold` ва `cancelled` **бевосита** тавассути PATCH иҷозат нест — барои ин endpoint-ҳои алоҳида мавҷуданд: `/reserve`, `/close`, `/cancel`. Сабаб: ин амалҳо танҳо статусро намерабонанд, балки квартираро низ идора мекунанд (резерв, фурӯш, озодкунӣ). Агар ин тавассути PATCH иҷро мешуд, квартира дар матритса нав намешуд — ин "fake logic" мебуд.

---

## 8. Резерватсия чӣ гуна кор мекунад

1. Менеҷер `POST /api/v1/deals/{id}/reserve` мефиристад.
2. Сервис санҷиш мекунад:
   - Деал дар статуси пеш-резерв аст (new/consultation/meeting/negotiation)
   - Деал квартира дорад
   - Квартира фурӯхта ё блокшуда нест
   - Квартираро деали дигари фаъол резерв накардааст
   - Барои ин квартира деали дубликати фаъол вуҷуд надорад
3. Статуси деал ба `reserved` меравад, `reservation_until` муқаррар мешавад (бо нобаёнӣ 7 рӯз).
4. `reservation_service.hold` квартираро дар матритса `reserved` мекунад ва `deal_id` -ро мегузорад.
5. Таймлайн `reservation` сабт мешавад.

**Дар бораи мӯҳлат:** ҳар вақт ягон endpoint даъват мешавад, сервис аввал резервҳои мӯҳлаташон гузаштаро бекор мекунад (`expire_overdue_reservations`). Агар мӯҳлат гузашта бошад, квартира автоматикӣ `available` мешавад ва боз фурӯхтан мумкин аст.

**Ҳифз дар сатҳи пойгоҳи додаҳо:** индекси қисман уникалӣ `uq_deals_active_apartment` наметузорад, ки ду деали фаъол барои як квартира вуҷуд дошта бошанд — ҳатто агар ду дархост ҳамзамон биёянд.

---

## 9. Пардохтҳо

`POST /api/v1/deals/{id}/payments`:
- Маблағ бояд мусбат бошад ва аз боқимонда зиёд набошад (`validate_payment_amount`)
- Дар деали фурӯхта ё лағвшуда пардохт иҷозат нест
- Пас аз пардохт `paid_amount` ва `remaining_amount` автоматикӣ нав мешаванд
- Ҳар пардохт дар таймлайн сабт мешавад (`payment_added` бо арзишҳои кӯҳна ва нав)

---

## 10. Ҳуҷҷатҳо

Боркунии ҳуҷҷат тавассути `multipart/form-data`:
- Намуди ҳуҷҷат (passport, contract, receipt, invoice, mortgage, photo, blueprint, pdf)
- Ном ва файл
- Файл дар `uploads/deals/{deal_id}/` нигоҳ дошта мешавад бо номи UUID (пешгирии ихтилофи ном)
- Ҳадди андоза: 50 МБ
- Ҳангоми ҳазфи сабт, файл низ аз диск тоза мешавад

---

## 11. Статистика ва Дашборд

### `GET /api/v1/deals/statistics`
- `total_deals` — шумораи умумӣ
- `open_deals` — деалҳои кушода (sold/cancelled нестанд)
- `reserved` — резервшуда
- `sold` — фурӯхта
- `cancelled` — лағвшуда
- `deals_today` — деалҳои имрӯз сохташуда
- `deals_this_month` — деалҳои ин моҳ сохташуда
- `revenue` — даромади умумӣ (ҷамъи final_price-и фурӯхта)
- `average_deal` — арзиши миёнаи деали фурӯхта
- `conversion_rate` — меъёри табдил (фурӯхта ÷ умумӣ)
- `top_managers` — рейтинги менеҷерон аз рӯи фурӯш
- `average_closing_time_days` — вақти миёнаи бастани деал (дар рӯз)

Барои менеҷер ҳамаи ин арзишҳо танҳо ба деалҳои худи ӯ ҳисоб карда мешаванд.

### `GET /api/v1/dashboard/deals`
Деалҳои охирин, амалҳои охирин, пардохтҳои охирин, вазифаҳои имрӯза, вазифаҳои дермонда, мулоқотҳои дарпешистода (48 соат дохили он), резервҳои мӯҳлаташон тамомшуда.

---

## 12. Ҳуқуқҳои дастрасӣ (Permissions)

Дар `app/permissions/roles.py` ҳуқуқҳои нави деал илова шудаанд: `deals.create`, `deals.view`, `deals.update`, `deals.delete`, `deals.reserve`, `deals.cancel`, `deals.close`, `deals.restore`, `deals.timeline`, `deals.activities`, `deals.tasks`, `deals.documents`, `deals.payments`, `deals.statistics`, `deals.dashboard`.

| Нақш | Ҳуқуқ |
|------|-------|
| **super_admin / admin** | Ҳамаи ҳуқуқҳо, ҳамаи деалҳоро мебинанд |
| **manager** | Ҳамаи ҳуқуқҳои деал, аммо танҳо ба деалҳои худаш (дар сервис `_ensure_access` санҷида мешавад) |
| **client** | Танҳо хондани деалҳои худаш ва танҳо активитҳои оммавӣ (`is_public=True`) |

Қоидаи муҳим: менеҷер наметавонад деалро ба менеҷери дигар гузаронад (403). Админ метавонад.

---

## 13. Шарҳи ҳар endpoint

### Деалҳо

| Метод | Роҳ | Шарҳ |
|-------|-----|------|
| `GET` | `/api/v1/deals` | Рӯйхат бо саҳифабандӣ, ҷустуҷӯ, филтр (status, priority, manager, project, building, apartment, payment_type, date_from, date_to), тартиб |
| `POST` | `/api/v1/deals` | Эҷоди деал (201). Аз лид ё мустақим |
| `GET` | `/api/v1/deals/statistics` | Омор (бинед боби 11) |
| `GET` | `/api/v1/deals/{id}` | Деал бо иртиботҳо |
| `PATCH` | `/api/v1/deals/{id}` | Таҳрири қисмӣ |
| `DELETE` | `/api/v1/deals/{id}` | Ҳазфи нарм |
| `POST` | `/api/v1/deals/{id}/reserve` | Резерватсияи квартира |
| `POST` | `/api/v1/deals/{id}/cancel` | Лағв бо сабаб |
| `POST` | `/api/v1/deals/{id}/close` | Бастани деал (sold) |
| `POST` | `/api/v1/deals/{id}/restore` | Эҳёи деали лағвшуда |

### Таймлайн / Активитҳо / Вазифаҳо

| Метод | Роҳ | Шарҳ |
|-------|-----|------|
| `GET` | `/api/v1/deals/{id}/timeline` | Таърихи пурраи рӯйдодҳо |
| `GET` | `/api/v1/deals/{id}/activities` | Активитҳо (клиент танҳо оммавиҳоро мебинад) |
| `POST` | `/api/v1/deals/{id}/activities` | Активити нав (201) |
| `PATCH` | `/api/v1/activities/{id}` | Таҳрири активит (танҳо муаллиф/админ) |
| `DELETE` | `/api/v1/activities/{id}` | Ҳазфи активит |
| `GET` | `/api/v1/deals/{id}/tasks` | Вазифаҳо |
| `POST` | `/api/v1/deals/{id}/tasks` | Вазифаи нав (201) |
| `PATCH` | `/api/v1/tasks/{id}` | Таҳрир/иҷрои вазифа |
| `DELETE` | `/api/v1/tasks/{id}` | Ҳазфи вазифа |

### Ҳуҷҷатҳо / Пардохтҳо

| Метод | Роҳ | Шарҳ |
|-------|-----|------|
| `GET` | `/api/v1/deals/{id}/documents` | Рӯйхати ҳуҷҷатҳо |
| `POST` | `/api/v1/deals/{id}/documents` | Боркунии ҳуҷҷат (multipart, 201) |
| `DELETE` | `/api/v1/documents/{id}` | Ҳазфи ҳуҷҷат |
| `GET` | `/api/v1/deals/{id}/payments` | Рӯйхати пардохтҳо |
| `POST` | `/api/v1/deals/{id}/payments` | Пардохти нав (201) |

### Дашборд

| Метод | Роҳ | Шарҳ |
|-------|-----|------|
| `GET` | `/api/v1/dashboard/deals` | Маълумоти дашборд |

---

## 14. Хатогиҳо (Error Handling)

- **400** — тасдиқи тиҷоратӣ: нархи манфӣ, тахфифи зиёд, гузариши ғайриимкон, мӯҳлати гузашта, пардохти зиёд
- **401** — токени беэътибор
- **403** — ҳуқуқ надорад: менеҷер деали дигар, клиент таҳрир кардан мехоҳад
- **404** — деал ё объекти иртиботӣ ёфт нашуд
- **409** — ихтилоф: деали дубликат барои квартира, квартираи фурӯхта/резервшуда
- **422** — вайронкунии тасдиқи Pydantic (майдонҳои набуда, намуди нодуруст)

---

## 15. Санҷишҳо (Tests)

Файли `tests/test_deals.py` ҳамаи endpoint-ҳоро санҷидааст:
- Эҷоди деал (аз лид, бе лид, таъини автоматикии менеҷер)
- Санҷиши дубликат, квартираи фурӯхта, тахфифи зиёд
- Резерв, лағв, бастан, эҳё
- Тағйири статус (гузариши ғайриимкон → 400)
- Таймлайн, активитҳо, вазифаҳо, ҳуҷҷатҳо, пардохтҳо
- Омор ва дашборд
- Ҳуқуқҳо: менеҷер деали дигарро намебинад, клиент танҳо хондан

**Хусусияти муҳими техникӣ:** дар `app/core/database.py` барои `Base` опсияи `eager_defaults=True` гузошта шудааст. Сабаб: майдонҳои `updated_at` (`onupdate=func.now()`) пас аз `flush()` қимати серверӣ мегиранд ва агар `eager_defaults` набошад, онҳо "expired" мешаванд — ҳангоми ҷавобдиҳӣ ба ин майдонҳо дастрасӣ боиси хатогии `MissingGreenlet` мегардад.

---

## 16. Хулоса

Модули Deal Management ба таври пурра сохта шудааст ва бо меъмории мавҷуда ҳамгино аст:
- 6 ҷадвал (деал ва ҳамаи бахшҳои он) бо UUID, индексҳо, калидҳои хориҷӣ, энумҳо
- Индекси қисман уникалӣ барои пешгирии ду деали фаъол барои як квартира
- Логикаи резерватсия бо озодкунии автоматикӣ
- Таймлайн (audit log) барои ҳар амал
- Пардохтҳо бо ҳисобкунии автоматикии боқимонда
- Омор ва дашборд
- Ҳуқуқҳои дақиқ барои ҳар нақш (админ, менeҷер, клиент)
- Ҷустуҷӯ, филтр, тартибдиҳӣ, саҳифабандӣ
- 121 санҷиши автоматикӣ, ки ҳама гузаштаанд
- Хатогиҳои HTTP ба тарзи дуруст
